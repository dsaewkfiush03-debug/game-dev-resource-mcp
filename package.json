{
  "name": "game-dev-resource-mcp",
  "version": "0.9.0",
  "description": "License-aware game development resource discovery for AI coding agents via MCP.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "game-dev-resource-mcp": "./dist/index.js"
  },
  "files": [
    "dist",
    "registry",
    "licenses"
  ],
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test src/licenses.test.ts src/providers/polyhaven.test.ts src/providers/catalog.test.ts src/providers/metadata.test.ts src/providers/v06.test.ts src/providers/v07.test.ts src/providers/v08.test.ts src/search.test.ts src/install.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
